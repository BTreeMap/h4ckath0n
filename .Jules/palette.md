## 2026-03-09 - [Inline Password Visibility Toggle] **Learning:** Complex universal form behaviors like password toggles should be built directly into design system primitives (like `Input.tsx`) rather than relying on developers to compose wrapper elements, ensuring automatic and consistent application of accessibility features. **Action:** Integrate common form accessibility requirements (clearable inputs, visibility toggles) into primitive components where possible to reduce boilerplate and enforce standards.